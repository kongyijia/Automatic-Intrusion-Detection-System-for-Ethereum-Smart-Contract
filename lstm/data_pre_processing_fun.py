# this function accepts raw data and transform it into legal format
# for each data set, there is a label stating the type of the source contract
# titles of table are removed during this process
# after processing, there are two elements for each item
# one is the label, the other is the data whose attributes are in clear order
# the first column of the data should be timestamp


def data_pre_processing(data):
    result = []
    return result
